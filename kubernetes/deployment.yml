apiVersion: apps/v1
kind: Deployment
metadata:
  name: aplicacao-deployment
spec:
  selector: # mysql Pod Should contain same labels
    matchLabels:
      app: aplicacao-pod
  template:
    metadata:
      labels:
        app: aplicacao-pod
    spec:
      containers:
        - name: container-minikube-helloword
          image: idasilva/minikube-helloword:1.0
          ports:
            - containerPort: 80

